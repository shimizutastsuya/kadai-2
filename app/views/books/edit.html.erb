<h1>編集画面</h1>
   <% if @book.errors.any? %>
    <div id="errors_container">
      <%= @book.errors.count %>errors prohibited this book from being saved:
      </br>
      <div id="errors_messages">
      <% @book.errors.full_messages.each do |message| %>
      <ul>
        <li><%= message %></li>
      </ul>
    <% end %>
      </div>
    </div>
    <% end %>
<!-- form_with部分 -->
<%= form_with model:@book, local: true do |f| %>
  <h4>タイトル</h4>
  <%= f.text_field :title %>
  <h4>本文</h4>
  <%= f.text_area :body %>
  <%= f.submit 'Update Book' %>
<% end %>

    <%= link_to 'Show', book_path(@book.id),class: 'btn btn-block btn-white'  %>
    |
    <%= link_to 'Back', books_path,class: 'btns btn-block btn-white'  %>